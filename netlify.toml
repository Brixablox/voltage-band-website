[build]
  command = "npm run build"
  publish = "dist"

# React Router client-side routing — send every path to index.html so
# direct loads/refreshes of routes like /about or /originals/burn work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
